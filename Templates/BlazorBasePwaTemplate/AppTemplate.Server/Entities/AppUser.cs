using BlazorBase.User.Server.Entities;

namespace AppTemplate.Server.Entities;

/// <summary>
/// The application's user. Empty on purpose — add project-specific profile fields here
/// (matching PodcastPortal's PodcastPortalUser, which adds playback preferences this way).
/// </summary>
public class AppUser : BaseUser
{
}
